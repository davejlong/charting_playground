%section#cities-content(data-city="#{@city}" data-handler='SeeClickFixVisualizer')
  %main
    %p Searching for #{params[:city] || 'Top 20 issues'} from #{link_to 'http://seeclickfix.com', 'SeeClickFix'}
    #map
